const initialState = {
  'projectId': null
}

export function manageProject (state = initialState, action) {
  let nextState

  switch (action.type) {
    case 'value':
      
      break;
  
    default:
      return state
      break;
  }

}